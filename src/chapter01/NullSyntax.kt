package chapter01

fun main(args: Array<String>) {
    println(" Need to declare ? if a variable need to assign null")

    var name : String? = null

    println("if a var is not declare with sign ? and assigned null then compile time error will occur")


}